[project]
name = "app"
version = "0.1.0"
description = "creating a cli absed agent for mac books"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.34.2",
    "typer>=0.26.8",
]
